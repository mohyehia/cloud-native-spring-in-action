apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: staging

resources:
  - ../../base

configMapGenerator:
  - behavior: merge
    files:
      - application-staging.yaml
    name: dispatcher-service-config

images:
  - name: dispatcher-service
    newName: dispatcher-service
    newTag: latest

patches:
  - path: patch-staging.yaml
    target:
      kind: Deployment
      name: dispatcher-service
