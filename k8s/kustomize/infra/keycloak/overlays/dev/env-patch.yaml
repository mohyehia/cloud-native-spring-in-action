apiVersion: apps/v1
kind: Deployment
metadata:
  name: workshop-keycloak
spec:
  template:
    spec:
      containers:
        - name: workshop-keycloak
          env:
            - name: KC_BOOTSTRAP_ADMIN_USERNAME
              value: admin
            - name: KC_BOOTSTRAP_ADMIN_PASSWORD
              value: admin
