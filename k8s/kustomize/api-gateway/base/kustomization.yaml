apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - api-gateway.yaml
configMapGenerator:
  - name: api-gateway-config
    files:
      - application.yaml
    options:
      labels:
        app: api-gateway