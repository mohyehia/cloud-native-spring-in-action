apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - order-service.yaml
configMapGenerator:
  - name: order-service-config
    files:
      - application.yaml
    options:
      labels:
        app: order-service