apiVersion: 1
providers:
  - name: 'My Applications'
    orgId: 1
    folder: 'Custom Dashboards'
    type: file
    disableDeletion: false
    editable: true
    options:
      # This MUST match the /etc/ path in your docker-compose volume
      path: /etc/grafana/provisioning/dashboards
