apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - catalog-service.yaml
configMapGenerator:
  - name: catalog-service-config
    namespace: dev
    files:
      - application.yaml
    options:
      labels:
        app: catalog-service